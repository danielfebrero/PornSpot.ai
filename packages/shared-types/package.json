{
  "name": "shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for PornSpot.ai frontend and backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "echo 'No linting configured for shared-types'",
    "lint:fix": "echo 'No linting configured for shared-types'",
    "test": "echo 'No tests configured for shared-types'",
    "prepublishOnly": "npm run build",
    "prepare": "npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "keywords": [
    "typescript",
    "types",
    "shared",
    "pornspot-ai"
  ],
  "author": "pornspot.ai",
  "license": "MIT",
  "private": true
}
